<?php

/**
 * Module ini harus paling awal dari semua module.
 */
function sbadmin2_library_install() {
  db_update('system')
    ->fields(array('weight' => -1))
    ->condition('name', 'sbadmin2_library', '=')
    ->execute();
}
